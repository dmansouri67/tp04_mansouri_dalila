import { ContactStateModel } from './contact-state-model';

describe('ContactStateModel', () => {
  it('should create an instance', () => {
    expect(new ContactStateModel()).toBeTruthy();
  });
});
